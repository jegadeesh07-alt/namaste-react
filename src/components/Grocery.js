const Grocery = () => {
  return (
    <div className="grocery-menu">
      <h1>
        this is our grocery page, when you need to buy grocery this it create's
        one js bundle for the grocery and it was when you click the grocery the
        only it loads the js bundler for the grocery
      </h1>
    </div>
  );
};

export default Grocery
